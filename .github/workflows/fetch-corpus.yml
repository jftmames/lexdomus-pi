name: "fetch-corpus — suspended pending T13"

on:
  workflow_dispatch:

permissions: {}

jobs:
  blocked:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before downloads, writes or model calls
        run: |
          echo "::error::Suspended pending T13 migration. No corpus prepared or activated. See docs/T13-workflows.md."
          exit 1
